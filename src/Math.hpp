#pragma once

#include "Header.hpp"

int max(int a, int b); // returns bigger of two numbers... wtf is this not already defined
int min(int a, int b); // min... you know what this does

int position_to_index_X(int X_pos);
int position_to_index_Y(int Y_pos);
